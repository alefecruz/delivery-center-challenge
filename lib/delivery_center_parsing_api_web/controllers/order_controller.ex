defmodule DeliveryCenterParsingApiWeb.OrderController do
  use DeliveryCenterParsingApiWeb, :controller

  alias DeliveryCenterParsingApiWeb.Orders

  def create(_conn, _params) do
    # render(conn, "order.json", order)
  end
end
